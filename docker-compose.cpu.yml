services:
  api:
    build:
      context: .
      dockerfile: docker/Dockerfile.cpu
    ports:
      - "8000:8000"
    environment:
      - VPG_DATA_DIR=/data
    volumes:
      - ./:/app
      - ./data:/data

